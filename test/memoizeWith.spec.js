"use strict";
const F = require("../index");
const assert = require("assert");

describe('test memoizeWith', () => {
    // it('memoizeWith', async () => {
    //     let counterIV = 0;
    //     const mustCallOnce = () => {
    //         if (counterIV++ !== 0) {
    //             assert.fail("must call once");
    //         }
    //         return 42;
    //     }
    //     const c = F.memoize(mustCallOnce);
    //     assert.strictEqual(42, await c());
    //     assert.strictEqual(42, await c());
    //     assert.strictEqual(42, await c());
    //     assert.strictEqual(42, await c());
    // });
});